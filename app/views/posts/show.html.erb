<main class='content container'>
  <ul>
    <li><%= render 'full', post: @post %></li>
    <% @post.comments.each do |comment| %>
      <li><%= render 'comment', comment: comment %></li>
    <% end %>
  </ul>
</main>